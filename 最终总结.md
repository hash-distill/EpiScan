# EpiScan 实验复现 —— 结果总结

> 论文：EpiScan: accurate high-throughput mapping of antibody-specific epitopes using sequence information
> npj Systems Biology and Applications (2024) 10:101
>
> GPU：NVIDIA RTX A6000 | 环境：episcan (Python 3.9, PyTorch 2.5.0+cu121)

---

## 一、实验总览


| 实验                         | 数据集               | 状态    | 关键结果                                           |
| ---------------------------- | -------------------- | ------- | -------------------------------------------------- |
| **DB1 单次训练**             | 103 训练 / 29 验证   | ✅ 完成 | AUC 0.638                                          |
| **DB1 5 折交叉验证**         | 162 复合物分 5 折    | ✅ 完成 | **AUC 0.673 ± 0.033**                             |
| **Custom 数据预处理**        | 1368 训练 / 156 测试 | ✅ 完成 | 全部 pickle/H5/TSV 就绪                            |
| **Custom 单次训练**          | 1368 → 156          | ✅ 跑通 | 1 epoch AUC≈0.60（调试期）                        |
| **Custom 5 折 CV（第一次）** | 1524 复合物分 5 折   | ✅ 完成 | AUC 0.630 ± 0.012（Best-MCC）                     |
| **Custom 5 折 CV（第二次）** | 1524 复合物分 5 折   | ✅ 完成 | **AUC 0.635 ± 0.012**（种子42，Best-MCC，可复现） |

---

## 二、DB1 基准实验

### 2.1 单次训练（103→29 划分）

训练命令：

```bash
python EpiScan/EpiScan/commands/train_sep-auc.py \
    --train ./dataProcess/public/public_sep_trainAg.tsv \
    --test ./dataProcess/public/public_sep_valAg.tsv \
    --embedding ./dataProcess/public/DB1.h5 \
    --lr 1e-4 --save-prefix ./save_model/DB1_ \
    --no-augment --device 0 --num-epochs 250 --batch-size 15 \
    -o ./output/DB1_training.log
```


| 指标     | 最佳值     | 最佳 Epoch | 对应其他指标                                |
| -------- | ---------- | ---------- | ------------------------------------------- |
| **AUC**  | **0.6384** | Epoch 32   | AUPR=0.226, F1=0.179, Prec=0.107, Rec=0.832 |
| **AUPR** | **0.2357** | Epoch 121  | AUC=0.628, F1=0.188                         |
| **F1**   | **0.1977** | Epoch 219  | AUC=0.593                                   |

**收敛特征**：AUC 在 Epoch 32 达峰后持续下降（过拟合）；验证集 Recall 从 0.81 升到 0.92，但 Precision 始终 ~0.11。

### 2.2 DB1 5 折交叉验证（162 复合物）

将 162 个复合物按表位比例分层分 5 折，每折约 130 训练 / 32 验证，各跑 250 epochs：


| Fold               | Best AUC           | Epoch | AUPR               | F1                 | Precision          | Recall             |
| ------------------ | ------------------ | ----- | ------------------ | ------------------ | ------------------ | ------------------ |
| 0                  | **0.7075**         | 233   | 0.326              | 0.201              | 0.116              | 0.994              |
| 1                  | **0.6443**         | 237   | 0.267              | 0.200              | 0.163              | 0.854              |
| 2                  | **0.6662**         | 167   | 0.285              | 0.213              | 0.123              | 0.990              |
| 3                  | **0.6321**         | 65    | 0.265              | 0.211              | 0.124              | 0.914              |
| 4                  | **0.7129**         | 39    | 0.359              | 0.206              | 0.136              | 0.748              |
| **均值 ± 标准差** | **0.673 ± 0.033** |       | **0.300 ± 0.037** | **0.206 ± 0.005** | **0.132 ± 0.017** | **0.900 ± 0.092** |

### 2.3 与论文对比


| 指标          | 单次训练 | 5 折 CV（我们）    | 论文（5 折 CV）    | 差距        |
| ------------- | -------- | ------------------ | ------------------ | ----------- |
| **AUROC**     | 0.638    | **0.673 ± 0.033** | **0.715 ± 0.008** | ↓ 0.042    |
| **AUPRC**     | 0.236    | **0.300 ± 0.037** | **0.304 ± 0.009** | ↓ 0.004 ✅ |
| **Precision** | 0.107    | **0.132 ± 0.017** | **0.239 ± 0.019** | ↓ 0.107    |
| **Recall**    | 0.832    | **0.900 ± 0.092** | **0.776 ± 0.038** | ↑ 0.124    |
| **F1**        | 0.198    | **0.206 ± 0.005** | **0.338 ± 0.021** | ↓ 0.132    |

**关键发现**：

1. **AUPR 已基本复现**（0.300 vs 0.304），5 折 CV 相比单次训练提升明显
2. **AUROC 差 0.042**（0.673 vs 0.715），主要来自 **Precision 偏低**（0.132 vs 0.239）
3. 模型倾向高 Recall 低 Precision：预测大量残基为表位，导致 Precision 低、F1 偏低
4. 剩余差距可能来自：随机种子未固定、损失权重差异（论文 0.9GD+0.05CC+0.05KL vs 代码 0.95Dice+0.05BCE）、特征质量（custom 的 PSSM 为零填充）

---

## 三、Custom 数据集实验

### 3.1 数据预处理（✅ 已完成）

```
dataProcess/custom/
├── pdb/                                  4742 个 PDB 文件
├── custom_all_proteins.fasta             9482 条全部蛋白序列
├── custom_all_proteins.h5                30 GB 全部蛋白嵌入 ⭐ 训练用
├── custom_pdb_dict_AgAb.pickle           9482 keys（抗原66维 + 抗体46维）
├── custom_cdr_dict.pickle                4741 个 CDR 标注
├── custom_catsite_dict.pickle            4741 个 VH/VL 切割位点
├── custom_train.tsv                      1368 训练对
├── custom_test.tsv                       156 测试对
└── custom_fold{0-4}_{train,val}.tsv      5 折划分
```

### 3.2 训练状态

- **单次训练已跑通**：1 epoch 完整训练 + 评估成功（AUC≈0.60），修复了全部 6 个 bug（详见复现任务.md 7.3 节）
- **5 折交叉验证共完成两次**（按时间先后）：
  - **第一次（2026-08-04 ~ 08-05）**：无固定种子，5 折并行跑在 5 张 GPU 上，AUC **0.630 ± 0.012**（按 Best MCC 选点）
  - **第二次（2026-08-10 ~ 08-11）**：加 `--seed 42` 固定随机种子，单 GPU 顺序跑完 5 折，AUC **0.635 ± 0.012**（按 Best MCC 选点）
- 两次划分方式相同：1524 复合物按表位比例分层分 5 折，每折约 1220 训练 / 305 验证，各跑 250 epochs

### 3.3 Custom 5 折交叉验证结果（两次，按时间划分）

#### 3.3.1 第一次 5 折 CV（2026-08-04 ~ 08-05，无固定种子）

5 折并行训练，按 **Best MCC 选点**（从各折 per-epoch 指标 CSV 回溯 MCC 最高时刻，即 `save_model` 缺 bestMCC 模型时用日志/CSV 重建）：


| Fold | Epoch | ACC   | AUC    | AUPR  | F1    | MCC   | Precision | Recall |
| ---- | ----- | ----- | ------ | ----- | ----- | ----- | --------- | ------ |
| 0    | 36    | 44.5% | 0.6218 | 0.242 | 0.179 | 0.071 | 0.121     | 0.668  |
| 1    | 207   | 53.2% | 0.6260 | 0.226 | 0.200 | 0.089 | 0.137     | 0.624  |
| 2    | 21    | 51.5% | 0.6424 | 0.241 | 0.199 | 0.095 | 0.136     | 0.649  |
| 3    | 71    | 52.4% | 0.6432 | 0.254 | 0.173 | 0.082 | 0.122     | 0.588  |
| 4    | 17    | 35.2% | 0.6161 | 0.222 | 0.202 | 0.072 | 0.128     | 0.779  |

**5 折均值 ± 标准差（按 Best MCC 选点）**：


| 指标          | 值                   |
| ------------- | -------------------- |
| **ACC**       | 0.4733 ± 0.0762     |
| **AUROC**     | **0.6299 ± 0.0123** |
| **AUPRC**     | **0.2371 ± 0.0130** |
| **F1**        | **0.1907 ± 0.0136** |
| **MCC**       | **0.0817 ± 0.0105** |
| **Precision** | **0.1289 ± 0.0077** |
| **Recall**    | 0.6616 ± 0.0720     |

#### 3.3.2 第二次 5 折 CV（2026-08-10 ~ 08-11，固定种子 42）

单 GPU 顺序训练，固定 `--seed 42`（可复现）。全部指标按 **Best MCC 选点**（训练时同时保存 `best_final.sav` 与 `bestMCC_final.sav`，本表用后者）：


| Fold | Epoch | ACC   | AUC    | AUPR  | F1    | MCC   | Precision | Recall |
| ---- | ----- | ----- | ------ | ----- | ----- | ----- | --------- | ------ |
| 0    | 15    | 40.6% | 0.6180 | 0.234 | 0.196 | 0.081 | 0.122     | 0.756  |
| 1    | 18    | 49.1% | 0.6352 | 0.232 | 0.199 | 0.101 | 0.130     | 0.680  |
| 2    | 47    | 51.5% | 0.6477 | 0.252 | 0.195 | 0.095 | 0.133     | 0.646  |
| 3    | 9     | 36.1% | 0.6483 | 0.241 | 0.193 | 0.071 | 0.116     | 0.787  |
| 4    | 7     | 38.0% | 0.6251 | 0.217 | 0.195 | 0.078 | 0.121     | 0.772  |

**5 折均值 ± 标准差（按 Best MCC 选点）**：


| 指标          | 值                   |
| ------------- | -------------------- |
| **ACC**       | 0.4308 ± 0.0614     |
| **AUROC**     | **0.6349 ± 0.0120** |
| **AUPRC**     | **0.2352 ± 0.0115** |
| **F1**        | **0.1955 ± 0.0019** |
| **MCC**       | **0.0852 ± 0.0111** |
| **Precision** | **0.1241 ± 0.0062** |
| **Recall**    | 0.7284 ± 0.0551     |

### 3.4 论文实验 / Custom 两次 5 折 CV 汇总对比

全部指标按 **Best MCC 选点**。论文实验为论文在 DB1 上报告的 5 折 CV 结果（论文未报告 ACC/MCC，以「—」表示；论文使用 Best AUC 选点报告排序类指标）；8 月 4 号、8 月 10 号为本项目在 Custom 上的两次复现。注：**DB1 章节（2.2/2.3）仍为 Best-AUC 选点**，因 DB1 训练（8/3）时脚本未输出每 epoch 指标、无法回溯 Best-MCC，且论文本身即按 Best-AUC 报告，故 DB1 保持与论文一致的选点方式可比。Custom 两列统一按 Best-MCC 报告。


| 指标          | 论文实验（DB1）    | 8 月 4 号（Custom，无种子） | 8 月 10 号（Custom，种子42） |
| ------------- | ------------------ | --------------------------- | ---------------------------- |
| **AUROC**     | **0.715 ± 0.008** | 0.630 ± 0.012              | **0.635 ± 0.012**           |
| **AUPRC**     | **0.304 ± 0.009** | 0.237 ± 0.013              | **0.235 ± 0.012**           |
| **Precision** | **0.239 ± 0.019** | 0.129 ± 0.008              | 0.124 ± 0.006               |
| **Recall**    | 0.776 ± 0.038     | 0.662 ± 0.072              | 0.728 ± 0.055               |
| **F1**        | **0.338 ± 0.021** | 0.191 ± 0.014              | 0.196 ± 0.002               |
| **ACC**       | —                 | 0.473 ± 0.076              | 0.431 ± 0.061               |
| **MCC**       | —                 | 0.082 ± 0.011              | 0.085 ± 0.011               |

**结论**：

1. **两次 Custom 训练高度一致**（AUROC 0.630 vs 0.635、MCC 0.082 vs 0.085，差异 < 0.01），证明 Custom 数据集上的训练稳定、可复现，固定种子未改变整体水平
2. **Best MCC 选点下阈值类指标大幅稳定**：ACC `0.47±0.08 / 0.43±0.06`、F1 `0.19±0.01`、MCC `0.08±0.01`，五折间几乎不再剧烈波动（对比 Best AUC 选点下 ACC 标准差达 ±0.22）；代价是 ACC 数值下降（不再被"全预测为负"的虚高 ACC 抬高）
3. **与论文（DB1）相比**：Custom 的 AUROC 低 ~0.08、AUPR 低 ~0.07、Precision 低 ~0.11、F1 低 ~0.14，主要原因是 Custom 抗体缺轻链（见 3.5 节）

### 3.4.1 独立测试集（156 对）结果

**第二次 5 折**（种子 42）的 **5 个 Best-MCC 模型**（`bestMCC_final.sav`）在 `custom_test.tsv`（156 对）上推理，指标口径与训练评估一致（ACC 为残基级、其余 per-sample 宏平均，阈值 0.5）。每折指标见 `output/20260813_mcc/testset_metrics.csv`，5 折均值如下：


| 指标          | 8 月 10 号 5 折（测试集，Best-MCC） |
| ------------- | ----------------------------------- |
| **ACC**       | 0.4489 ± 0.0735                    |
| **AUROC**     | **0.6349 ± 0.0047**                |
| **AUPRC**     | **0.2362 ± 0.0047**                |
| **Precision** | **0.1250 ± 0.0082**                |
| **Recall**    | 0.6924 ± 0.0886                    |
| **F1**        | **0.1902 ± 0.0125**                |
| **MCC**       | **0.0807 ± 0.0105**                |

> ⚠️ **第一次 5 折（8 月 4 号）无 Best-MCC 测试集结果**：当时训练脚本尚未加入 bestMCC 保存功能，只有 Best-AUC 模型（`best_final.sav`），无法按 Best-MCC 选点推理独立测试集；其验证集 Best-MCC 指标可从 per-epoch CSV 回溯（见 3.3.1）。

**要点**：

1. **独立测试集（Best-MCC 模型）AUROC 0.635 与验证集 Best-MCC AUC 0.635 完全持平**、MCC 0.081 vs 0.085，说明模型在未见过的 156 对上无过拟合，泛化能力与验证集一致
2. 对比 Best-AUC 模型在测试集上的结果（AUROC 0.644、MCC 0.049，见 `output/20260813/testset_metrics.csv`），Best-MCC 模型把 MCC 从 0.049 提升到 0.081、F1 从 0.123 提升到 0.190，阈值类指标明显更可靠
3. 相比之前 `custom_full` 模型（修复 `model.eval()` 前）在测试集上的 AUC 0.585，修复 dropout bug 后各 fold 模型 AUC 提升到 0.63 左右，说明此前的独立测试结果被 dropout 随机性拉低了

### 3.5 关键发现：Custom 数据缺轻链

⚠️ **Custom 数据集的抗体只有 VH 单链，没有轻链（VL）**，这是结果系统性低于 DB1 的根本原因：


|          | DB1 抗体                     | Custom 抗体                      |
| -------- | ---------------------------- | -------------------------------- |
| 序列长度 | 428-439 aa（VH+CH1+VL 全长） | 115-122 aa（仅 VH 单链）         |
| 轻链 VL  | ✅ 完整                      | ❌ 0 条（4741 个抗体中 VL 为 0） |

- EpiScan 模型**假设输入是 VH+VL 全长**，按 catsite 切分后分别用 VH 和 VL 计算接触图
- Custom 数据只有 VH，模型把这条单链强行切成两半，后半段（实为 VH 的 CDR3+FR4）被当成"假 VL"处理
- 因此模型从未见过真实的轻链特征，导致 AUC 系统性偏低 0.037

**备注**：第一次 Fold 2 在 Best AUC 选点（E199）时指标异常（F1=0.023, Rec=0.021，几乎全预测为负），Best MCC 选点（E21）则 F1=0.199、MCC=0.095，正常得多。这是两种选点方式差异的典型例证：Best AUC 时刻模型在 0.5 阈值下常近乎全预测为负，排序指标 AUC 仍高，但阈值类指标失真（见 3.6）。全文指标统一按 Best MCC 选点报告，以得到稳定的阈值类指标。

### 3.6 MCC 与选点方式说明

**为何全文按 Best MCC 选点报告：**

1. **AUC 是排序指标，MCC 是阈值指标**。按 "Best AUC" 选点时，该时刻模型在 0.5 阈值下常近乎全预测为负（如第一次 Fold 2 E199：Prec=0.039, Rec=0.021），AUC 仍高但 MCC≈0、ACC 虚高（可达 90%），阈值类指标完全失真。**改用 Best MCC 选点后，阈值类指标大幅稳定**：ACC 五折从 `±0.22` 收窄到 `±0.07`，MCC 从 ~0.048 提升到 **~0.082~0.085**，F1 从 ~0.12 提升到 ~0.19，更能反映模型在 0.5 阈值下的真实水平。
2. **代价**：Best MCC 选点下排序类指标略降（AUROC 0.636→0.630、AUPR 0.239→0.237），ACC 数值下降（不再被"全预测为负"抬高）——这是选点方式切换的正常现象，不代表模型变差。
3. **Precision 仍偏低**：表位仅占 ~10%，模型倾向"多报不漏报"（高 Recall 低 Precision），而 MCC 同时惩罚假阳性与假阴性。Best MCC 选点下 Precision 为 0.125~0.129（论文 0.239），MCC 仍偏低。根源是 Custom 抗体缺轻链、特征质量有限（PSSM 零填充）。

### 3.7 Custom 训练命令

单次训练：

```bash
python EpiScan/EpiScan/commands/train_sep-auc.py \
    --train ./dataProcess/custom/custom_train.tsv \
    --test ./dataProcess/custom/custom_test.tsv \
    --embedding ./dataProcess/custom/custom_all_proteins.h5 \
    --pdb-dict ./dataProcess/custom/custom_pdb_dict_AgAb.pickle \
    --cdr-dict ./dataProcess/custom/custom_cdr_dict.pickle \
    --lr 1e-4 --save-prefix ./save_model/custom_ \
    --no-augment --device 0 --num-epochs 250 --batch-size 15 \
    -o ./output/custom_training.log
```

5 折交叉验证（第二次，固定种子，单 GPU 顺序跑）：

```bash
export PYTHONPATH=/mnt/Data4/24zhs/EpiScan/EpiScan
PY=/home/24xyx/miniconda3/envs/episcan/bin/python

for fold in 0 1 2 3 4; do
    CUDA_LAUNCH_BLOCKING=1 $PY EpiScan/EpiScan/commands/train_sep-auc.py \
        --train ./dataProcess/custom/custom_fold${fold}_train.tsv \
        --test  ./dataProcess/custom/custom_fold${fold}_val.tsv \
        --embedding ./dataProcess/custom/custom_all_proteins.h5 \
        --pdb-dict ./dataProcess/custom/custom_pdb_dict_AgAb.pickle \
        --cdr-dict ./dataProcess/custom/custom_cdr_dict.pickle \
        --lr 1e-4 --save-prefix ./save_model/custom_cv_seed42_fold${fold}_ \
        --no-augment --device 2 --num-epochs 250 --batch-size 15 \
        -o ./output/custom_cv_seed42_fold${fold}.log \
        --out-metrics ./output/custom_cv_seed42_fold${fold}_metrics.csv \
        --seed 42
done
```

> ⚠️ `--seed 42` 固定随机种子，结果可复现；脚本同时保存 Best AUC（`best_final.sav`）与 Best MCC（`bestMCC_final.sav`）模型。
> ⚠️ 必须用 `custom_all_proteins.h5`（含全部抗原+抗体），不能用 `custom_antibodies.h5`。
> ⚠️ 每次运行结果自动归档到 `output/<日期>/` 和 `save_model/<日期>/`，互不覆盖。
> ⚠️ 日志默认保存在 `output/<日期>/` 下，汇总时需指向日期目录。

独立测试集推理（用 Best-MCC 模型，5 折各一次，结果汇总到 `output/<日期>/testset_metrics.csv`）：

```bash
export PYTHONPATH=/mnt/Data4/24zhs/EpiScan/EpiScan
PY=/home/24xyx/miniconda3/envs/episcan/bin/python
$PY EpiScan/EpiScan/commands/testset_cv.py \
    --test ./dataProcess/custom/custom_test.tsv \
    --embedding ./dataProcess/custom/custom_all_proteins.h5 \
    --pdb-dict ./dataProcess/custom/custom_pdb_dict_AgAb.pickle \
    --cdr-dict ./dataProcess/custom/custom_cdr_dict.pickle \
    --checkpoints \
      ./save_model/20260810/custom_cv_seed42_fold0_bestMCC_final.sav \
      ./save_model/20260811/custom_cv_seed42_fold1_bestMCC_final.sav \
      ./save_model/20260811/custom_cv_seed42_fold2_bestMCC_final.sav \
      ./save_model/20260811/custom_cv_seed42_fold3_bestMCC_final.sav \
      ./save_model/20260811/custom_cv_seed42_fold4_bestMCC_final.sav \
    --outdir output/20260813_mcc --device 1
```

---

## 四、输出归档机制

每次训练运行，日志/模型/指标自动存入日期目录：

```
output/<YYYYMMDD>/xxx.log          # 训练日志
output/<YYYYMMDD>/xxx_metrics.csv  # 指标 CSV
save_model/<YYYYMMDD>/xxx_best_final.sav     # Best AUC 主模型
save_model/<YYYYMMDD>/xxx_bestSeq_final.sav  # Best AUC DeepPPI
save_model/<YYYYMMDD>/xxx_bestMCC_final.sav     # Best MCC 主模型（种子42起）
save_model/<YYYYMMDD>/xxx_bestMCCSeq_final.sav  # Best MCC DeepPPI
```

启动时打印归档路径。同一天多次运行会覆盖，如需更细粒度可改为 `YYYYMMDD_HHMMSS`。

---

## 五、待办与建议


| 优先级 | 操作                               | 说明                                                                                                   |
| ------ | ---------------------------------- | ------------------------------------------------------------------------------------------------------ |
| ⭐⭐⭐ | **补充 Custom 抗体轻链序列**       | 当前无 VL 是结果偏低根本原因，从 PDB 提取配对 L 链拼接 VH+VL                                           |
| ⭐⭐   | 尝试提升 Precision                 | 当前高 Recall 低 Precision，可调 loss 权重或阈值                                                       |
| ⭐     | 修复`summarize_cv.py` 支持日期目录 | 当前只搜`output/` 根目录，需递归日期子目录                                                             |
| ⭐     | DB1 测试集推理                     | 用最优 checkpoint 在 30 对测试集上评估                                                                 |
| ⭐     | 补跑第一次 5 折 Best-MCC 测试集    | 第一次（8/4）训练时无 bestMCC 保存功能，如需其 Best-MCC 测试集结果需重训（或用 Best-AUC 模型结果替代） |

---

> **更新日期**：2026-08-13
> **状态**：DB1 基准实验完成（单次 + 5 折 CV）；Custom 预处理完成、5 折 CV 完成两次（无种子 AUC 0.630±0.012 / 种子42 AUC 0.635±0.012，均按 Best MCC 选点，结果可复现）；独立测试集（156 对）：第二次 5 折 Best-MCC 模型推理完成（AUC 0.635±0.005、MCC 0.081±0.011，与验证集持平），第一次 5 折因训练时无 bestMCC 模型暂缺
